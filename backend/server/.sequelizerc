const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'database_orm', 'config', 'database.js'),
    'migration-path': path.resolve(__dirname, 'database_orm', 'migrations'),
}